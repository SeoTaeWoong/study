package variable;

import java.util.ArrayList;

public class info {

	
	
	
	
	// 렙, 피, 마나, 공격력, 방어력, 스피드  // , 경험치, 이름, 캐릭 넘버링 , 세이브코드 , 착용아이템(5개)(투구, 갑옷, 무기, 방패, 신발), 
	
	
	
	

	
	
	int LV = 1;
	int hp = 0;
	int mp = 0;
	int atack = 0;
	int defence = 0;
	int speed = 0;
	
//	int statpoint;
//	int statpoint;

/*	int code(int a) {
		
		Cinfo.add(new String[] {,"b"});
		return a;
	}

	String name1(String i) {
		name = i;
		return name;
	}

	int Char(int choice, int num, int stat) {
		if (choice == 1 && num == 0001) {
			hp = hp + stat;
			return hp;
		} else if (choice == 2 && num == 0001) {
			mp = mp + stat;
			return mp;
		} else if (choice == 3 && num == 0001) {
			atack = atack + stat;
			return atack;

		} else if (choice == 4 && num == 0001) {
			defence = defence + stat;
			return defence;

		} else if (choice == 5 && num == 0001) {
			speed = speed + stat;
			return speed;
		}else
			return 0;

	}
*/
}
